name: waradah
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  shared_preferences: ^2.5.3
  provider: ^6.1.5 # إضافة Provider لإدارة الحالة
  logger: ^1.0.0 # إضافة Logger لتسجيل الأخطاء
  intl: ^0.18.0 # إضافة Intl لتنسيق العملات والأرقام

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/google_logo.png
    - assets/images/gift_212.png